# {{ project_name | title }} Telegram Bot

This is a Telegram Bot project generated by the `codex-bot` framework.

## 🚀 Quick Start

1.  **Navigate to the project directory:**
    ```bash
    cd {{ project_name }}
    ```

2.  **Create a virtual environment and install dependencies:**
    ```bash
    python -m venv .venv
    source .venv/bin/activate  # On Windows: .venv\Scripts\activate
    pip install -e .
    ```

3.  **Configure your bot:**
    Copy the example environment file and fill in your bot token:
    ```bash
    cp .env.example .env
    # Open .env and add your BOT_TOKEN
    ```

4.  **Run the bot:**
    ```bash
    python -m {{ project_name }}.app_telegram
    ```
    Or, if installed:
    ```bash
    run-bot
    ```

## ⚙️ Configuration

Edit `src/{{ project_name }}/core/settings.py` for static settings.
Edit `.env` for sensitive data (BOT_TOKEN, REDIS_URL, DATABASE_URL).

## ✨ Features

This bot includes:
-   **Data Mode:** `{{ data_mode | title }}`
-   **Redis:** `{% if use_redis %}Enabled{% else %}Disabled{% endif %}`
-   **i18n:** `{% if use_i18n %}Enabled{% else %}Disabled{% endif %}`
-   **Logging:** `{% if use_loguru %}Loguru (with masking){% else %}Standard logging{% endif %}`

## 📚 Documentation

For more information on `codex-bot` framework, visit:
[https://codexdlc.github.io/codex-bot/](https://codexdlc.github.io/codex-bot/)

---
*Generated by `codex-bot` v{{ lib_version }} on {{ timestamp }}*
